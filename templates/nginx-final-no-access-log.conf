# nginx final benchmark access-log suppression template
# Use only for final score measurement after analysis is complete.
# alp cannot analyze requests while access_log is disabled.

access_log off;
