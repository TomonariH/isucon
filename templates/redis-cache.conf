# Redis cache-oriented competition template
# Use only when Redis is a cache or rebuildable session store, not the source of
# truth. Disables persistence for benchmark throughput.

save ""
appendonly no
tcp-backlog 511
timeout 0
tcp-keepalive 60
maxclients 10000
maxmemory 512mb
maxmemory-policy allkeys-lru
